import {
  MoreVertical,
  Cloud,
  Server,
  Database,
  Pencil,
  Trash2,
} from "lucide-react";
const vendorMeta = {
  AWS: { icon: Cloud, badgeColor: "bg-yellow-500" },
  GCP: { icon: Server, badgeColor: "bg-red-500" },
  Azure: { icon: Database, badgeColor: "bg-blue-500" },
};

export default vendorMeta;
